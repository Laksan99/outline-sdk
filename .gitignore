# vscode
.trunk
.vscode


# MacOS files
.DS_Store
.idea

# Xcode
App.xcscheme

# Android Studio
output-metadata.json

# Not using Zero-Installs
.yarn/*
!.yarn/releases
!.yarn/plugins
.pnp.*
.yarn-integrity

#  using Zero Installs
#.yarn/unplugged
#.yarn/build-state.yml

# Tasks
.task
out

# Binaries built in place by `go build ./tools/<name>`, which lands the
# executable in the module root rather than the package directory.
/x/fetch
/x/fetch-speed
/x/resolve
/x/test-connectivity
/x/http2transport
